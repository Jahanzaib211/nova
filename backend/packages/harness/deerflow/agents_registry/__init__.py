"""Agent registry: one read model over the lead agent, built-in and custom
subagents, and ACP agents, with live queued/running counts from the job
runner. No table of its own — config + jobs are the sources of truth."""
