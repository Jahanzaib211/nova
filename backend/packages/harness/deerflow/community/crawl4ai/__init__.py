"""Crawl4AI integration: a real crawler, as distinct from a page fetcher.

`web_fetch` (Browserless) renders one URL that the agent already names.
`web_crawl` starts at a URL and *follows links*, which is a different job:
"read this page" versus "read this documentation site". Nova had no tool for
the second until now.
"""
