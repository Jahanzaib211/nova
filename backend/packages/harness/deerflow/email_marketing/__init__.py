"""Nova email marketing (harness side).

Phase 0 ships only the event vocabulary so the cross-language contract in
``contracts/email_marketing_events_contract.json`` is load-bearing from
day one. Models, templates, the SMTP pipeline and the bounce poller land
in a later phase.
"""
