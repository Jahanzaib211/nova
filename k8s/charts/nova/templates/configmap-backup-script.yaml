apiVersion: v1
kind: ConfigMap
metadata:
  name: nova-backup-script
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "nova.labels" . | nindent 4 }}
data:
  backup.py: |-
{{ .Files.Get "files/backup.py" | indent 4 }}
